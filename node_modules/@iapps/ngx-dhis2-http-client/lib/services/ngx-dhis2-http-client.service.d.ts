import { HttpClient } from '@angular/common/http';
import { ManifestService } from './manifest.service';
import { SystemInfoService } from './system-info.service';
import { Observable } from 'rxjs';
import { HttpConfig } from '../models/http-config.model';
import { IndexDbService } from './index-db.service';
export declare class NgxDhis2HttpClientService {
    private httpClient;
    private manifestService;
    private systemInfoService;
    private indexDbService;
    constructor(httpClient: HttpClient, manifestService: ManifestService, systemInfoService: SystemInfoService, indexDbService: IndexDbService);
    get(url: string, httpConfig?: HttpConfig): Observable<any>;
    post(url: string, data: any, httpConfig?: HttpConfig): Observable<any>;
    put(url: string, data: any, httpConfig?: HttpConfig): Observable<any>;
    delete(url: string, httpConfig?: HttpConfig): Observable<any>;
    private _get(url, httpConfig);
    private _handleError(err);
    private _getSanitizedRootUrl(httpConfig);
    private _getApiRootUrl(includeVersionNumber?, preferPreviousVersion?);
    private _updateIndexDb(httpConfig, requestData, action?);
    private _fetchFromIndexDb(httpConfig);
}
