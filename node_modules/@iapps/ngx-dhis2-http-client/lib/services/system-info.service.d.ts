import { Observable } from 'rxjs';
import { ManifestService } from './manifest.service';
import { HttpClient } from '@angular/common/http';
export declare class SystemInfoService {
    private manifestService;
    private httpClient;
    private _systemInfoLoaded;
    private _systemInfo;
    constructor(manifestService: ManifestService, httpClient: HttpClient);
    getSystemInfo(): Observable<any>;
    getSystemVersion(): Observable<number>;
}
