import { FilterByBookmarkPipe } from './filter-by-bookmark.pipe';

describe('FilterByBookmarkPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByBookmarkPipe();
    expect(pipe).toBeTruthy();
  });
});
